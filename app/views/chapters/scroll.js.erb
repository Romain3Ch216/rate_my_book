$(document).ready(function() {
  console.log('test1')
  $(window).scroll(function() {
    var scroll = $(window).scrollTop();
    $('.bookmark').click(function() {
      console.log('test');
      $.post('scolls/<%= @scroll.id %>', {scroll_value: scrol}),
        function(returnedData){
         console.log(returnedData);
     });
    });
  });
});
